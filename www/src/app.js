// Boorker Watchdog SPA — Entry Point
const app = document.getElementById('app');
app.removeAttribute('aria-busy');
app.removeAttribute('data-spinner');
app.textContent = 'Boorker Watchdog — SPA loading...';
